[packages.fuel-core-nats]
versioned_files = ["crates/fuel-core-nats/Cargo.toml"]
changelog = "CHANGELOG.md"
extra_changelog_sections = [{ name = "Documentation", types = ["docs"] }]

[[packages.fuel-core-nats.assets]]
path = "artifacts/fuel-core-nats-x86_64-unknown-linux-gnu.tgz"

# ------------------------------------------------------------
# Workflow to get the current version
# ------------------------------------------------------------
[[workflows]]
name = "get-version"
help_text = "Get the current version of the a project"

[[workflows.steps]]
type = "Command"
command = "echo \"$VERSION\""
variables = { "$VERSION" = "Version" }

# ------------------------------------------------------------
# Workflow to create a new changeset
# ------------------------------------------------------------
[[workflows]]
name = "changeset"

[[workflows.steps]]
type = "CreateChangeFile"

# ------------------------------------------------------------
# Workflow to create a new release
# ------------------------------------------------------------
[[workflows]]
name = "prepare-release"

[[workflows.steps]]
type = "PrepareRelease"

[[workflows.steps]]
type = "Command"
command = "git switch -c $RELEASE_BRANCH"
shell = true

[[workflows.steps]]
type = "Command"
command = "./scripts/bump-version.sh $VERSION && git add ."
variables = { "$VERSION" = "Version" }
shell = true

[[workflows.steps]]
type = "Command"
command = "git commit -m \"ci(release): preparing $VERSION\""
variables = { "$VERSION" = "Version" }

[[workflows.steps]]
type = "Command"
command = "git push --force --set-upstream origin $RELEASE_BRANCH"
shell = true

[[workflows.steps]]
type = "CreatePullRequest"
base = "main"
variables = { "$VERSION" = "Version" }

[workflows.steps.title]
template = "ci(release): preparing v$VERSION"
variables = { "$VERSION" = "Version" }

[workflows.steps.body]
template = "v$VERSION"
variables = { "$VERSION" = "Version" }

# ------------------------------------------------------------
# Workflow to release a new version
# ------------------------------------------------------------
[[workflows]]
name = "release"

[[workflows.steps]]
type = "Release"
variables = { "$VERSION" = "Version" }

# ------------------------------------------------------------
# GitHub configuration
# ------------------------------------------------------------
[github]
owner = "pedronauck"
repo = "rust-test"

[bot.releases]
enabled = true
